version: '3.8'
services:
  db:
    image: postgres:15
    container_name: halisaha_db_container
    environment:
      POSTGRES_DB: halisaha_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: admin123
    ports:
      - "5433:5432"
    restart: always
